

<!-- 投稿・更新時にサクセスメッセージ表示 -->
<% if flash[:notice] %>
	<div class = "flash">
		<%= flash[:notice] %>
	</div>
<% end %>


<p>Title: <%= @book.title %></p>
<p>Opinion : <%= @book.opinion  %></p>

<!-- 編集画面にアクセス -->
<%= link_to "Edit", edit_book_path(@book.id) %>
|
<!-- 投稿・一覧画面に戻る -->
<%= link_to 'Back', '/books' %>